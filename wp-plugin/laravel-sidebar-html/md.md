# MVC plugin
```md
Thành phần trong Plugin của bạnVai trò trong kiến trúc MVC / LaravelHook admin_menuTương đương với file routes/web.php (Định nghĩa URL).render_..._editĐóng vai trò là Controller (Hàm edit() để hiển thị form).admin_post_...Đóng vai trò là Controller (Hàm update() để xử lý logic).loader_...Đóng vai trò là Model (Tương tác với database/options).views/ folderĐóng vai trò là Blade Templates (Chỉ chứa HTML và biến).
```